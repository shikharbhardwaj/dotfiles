# Suppress the default "Welcome to fish, the friendly interactive shell"
# greeting. Defined as an empty function so nothing is printed on startup.
function fish_greeting --description 'Suppress the fish welcome message'
end
